// AIDEV-NOTE: donate-snippet.js is vendored plain JS (never modified, so its provenance
// stays obvious); this ambient declaration is the minimal typing tsc needs to import it.
export function renderDonateLink(): HTMLAnchorElement;
